#include "common/Scheduler.hpp"
#include "common/Assignment.hpp"
#include <iostream>

namespace SDMS
{  
  bool Scheduler::setPriority(Assignment assignment) const
  {
    //this will become an if assignment not a if true
    //if(assignment)
    if(true)
    {
      return true;
    }
    else 
    {
      return false; 
    }
  }
}
